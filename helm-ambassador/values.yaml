ambassador:
  replicaCount: 1
  service:
    type: NodePort
    ports:
      - name: https
        port: 443
        targetPort: 8443
        protocol: TCP
        nodePort: 30443

  enableAES: true
  licenseKey:
    value:
    createSecret: true
    secretName:

  redis:
    create: false

  authService:
    create: true

  rateLimit:
    create: false
