ambassador:
  replicaCount: 3
  service:
    type: NodePort
    ports:
      - name: https
        port: 443
        targetPort: 8443
        protocol: TCP
        nodePort: 30443
  adminService:
    type: NodePort
    nodePort: 32677

  enableAES: true
  licenseKey:
    value:
    createSecret: true
    secretName:

  redis:
    create: false

  authService:
    create: false

  rateLimit:
    create: false
